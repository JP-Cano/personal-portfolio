# ============================================
# Server Configuration
# ============================================

# Database Configuration
# =====================
# Choose database driver: 'sqlite' for local development or 'turso' for production
DB_DRIVER=sqlite

SERVER_PORT=8080

# SQLite Configuration (Local Development)
# =========================================
DATABASE_PATH=portfolio.db

# Turso Configuration (Production)
# =================================
# Get these values from: https://turso.tech/
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here

# Application Configuration
# ==========================
DEBUG=false

# Swagger UI (API docs at /swagger/index.html)
# ============================================
# Local: true (access via http://localhost:8080/swagger/index.html when
# running the backend directly — not proxied through Caddy)
# Production: false (keeps the OpenAPI surface private)
ENABLE_SWAGGER=true

# CORS Configuration
# ==================
# Local: http://localhost:3000,http://localhost:4321
# Production: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4321


# ============================================
# Frontend Configuration
# ============================================

# Frontend Port
PORT=3000

# Backend API URL (used by frontend SSR to communicate with backend)
# ⚠️ IMPORTANT: This is used by Astro server-side, not browser!
#
# Without Docker: http://localhost:8080/api/v1 (local dev machine)
# With Docker: http://backend:8080/api/v1 (Docker internal network)
PORTFOLIO_BACKEND_URL=http://backend:8080/api/v1

# Environment
ENVIRONMENT=local

# ============================================
# Deployment Configuration
# ============================================

# Site address for Caddy. Leave unset/empty for local development (Caddy
# serves plain HTTP on :80, no TLS). In production, set to your real
# domain(s), space-separated (NOT comma-separated — Caddyfile site
# addresses are whitespace-delimited), e.g.:
# SITE_ADDRESS="yourdomain.com www.yourdomain.com"
SITE_ADDRESS=

# Email for Let's Encrypt certificate registration (used by Caddy)
SSL_EMAIL=your-email@example.com

# Public Base URL (used for generating certification file URLs)
# ===============================================================
# This is the public-facing URL that browsers will use to access files
# Local: http://localhost (without Docker) or http://localhost:8080 (with Nginx proxy)
# Production: https://yourdomain.com
PUBLIC_BASE_URL=http://localhost:8080

# ============================================
# Authentication Configuration
# ============================================

# Session Cookie Name
# ====================
# Custom name for the session cookie (adds security by obscurity)
SESSION_COOKIE_NAME=portfolio_session

# Cookie Security Settings
# ========================
# COOKIE_SECURE: Set to 'true' in production (HTTPS only)
# - 'true': Cookies only sent over HTTPS
# - 'false': Cookies sent over HTTP (local development)
COOKIE_SECURE=false

# COOKIE_DOMAIN: The domain the cookie is valid for
# - Leave empty for localhost
# - Set to your domain in production (e.g., yourdomain.com)
COOKIE_DOMAIN=

# COOKIE_SAME_SITE: Controls cross-site cookie behavior
# - 'lax': Default, prevents CSRF while allowing some cross-site usage
# - 'none': For cross-origin requests (requires HTTPS and COOKIE_SECURE=true)
COOKIE_SAME_SITE=lax
