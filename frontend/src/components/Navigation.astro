---
// Thin Astro wrapper: resolves the CV asset URL (an Astro-only import) and
// mounts the interactive nav island. The logo is now an inline SVG inside the
// island, so no image assets are imported here anymore.
import cvFile from "@/assets/Juan-Pablo-Cano-resume-public.docx";
import NavigationBar from "@/components/NavigationBar.svelte";
---

<NavigationBar client:load cvHref={cvFile} />
