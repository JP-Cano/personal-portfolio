# Use public npm registry only for this project
# This overrides any global ~/.npmrc configuration
registry=https://registry.npmjs.org/

# Ensure all scoped packages use public registry too
@astrojs:registry=https://registry.npmjs.org/
@types:registry=https://registry.npmjs.org/

# Clear any auth tokens (forces public registry)
//registry.npmjs.org/:_authToken=
